import { Service } from "../types/types";

export interface ProductsProps {
  data: Service[];
  type: string;
}
export interface HeroProps {
  type: string;
  title: string;
  span: string;
  subtitle: string;
  image: string;
}
